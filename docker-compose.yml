services:
  backend:
    build: ./backend
    container_name: task-manager-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: task-manager-frontend
    ports:
      - "8082:80"
    depends_on:
      - backend

